# ⚡ ANTI-GRAVITY DATA COLLECTION SYSTEM
## GOD MODE ULTRA PRO MAX ∞
### World's Most Complete Free Data Collection Prompt
### Inspired by: Dario Amodei × Jensen Huang × Sam Altman × Yann LeCun × Elon Musk × Sundar Pichai × Zuckerberg × Karpathy × Einstein × Turing

---

## 🧬 PHILOSOPHICAL FOUNDATION — CEO MINDSET MATRIX

```
╔══════════════════════════════════════════════════════════════════╗
║  DARIO AMODEI (Anthropic) — "Machines of Loving Grace" (2024)  ║
║  ─────────────────────────────────────────────────────────────  ║
║  "A country of geniuses in a data center — working together    ║
║   to compress decades of human progress into years."           ║
║                                                                  ║
║  DIRECTIVE: Collect data as if each byte could cure a disease.  ║
║  → Biology data, medical research, mental health corpora        ║
║  → Every paper that could accelerate scientific discovery       ║
║  → Data that represents ALL of human knowledge                  ║
╚══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════╗
║  JENSEN HUANG (NVIDIA) — "Accelerated Everything"               ║
║  ─────────────────────────────────────────────────────────────  ║
║  "Software is eating the world. AI is eating software."         ║
║  DIRECTIVE: Maximize throughput. GPU time is sacred.            ║
║  → Batch everything. Parallelize everything.                    ║
║  → Every byte must earn its compute budget                      ║
╚══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════╗
║  SAM ALTMAN (OpenAI) — "The Intelligence Age"                   ║
║  ─────────────────────────────────────────────────────────────  ║
║  "We are building something that could be the most              ║
║   transformative technology in human history."                  ║
║  DIRECTIVE: Scale is not optional. It is the answer.            ║
║  → More data. Better data. Diverse data.                        ║
╚══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════╗
║  YANN LECUN (Meta AI) — "World Models"                          ║
║  ─────────────────────────────────────────────────────────────  ║
║  "The next AI revolution will come from self-supervised         ║
║   learning on world models — not language alone."               ║
║  DIRECTIVE: Collect ALL modalities. Text is not enough.         ║
║  → Video, audio, 3D, sensor data, embodied experience           ║
╚══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════╗
║  ELON MUSK (xAI/Tesla) — "First Principles"                     ║
║  ─────────────────────────────────────────────────────────────  ║
║  "Question every assumption. If it's not illegal and            ║
║   it works — do it 10x faster."                                 ║
║  DIRECTIVE: Ignore conventional limits. Mine everything.        ║
╚══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════╗
║  ANDREJ KARPATHY — "nanoGPT Clarity"                            ║
║  ─────────────────────────────────────────────────────────────  ║
║  "Understand every single line. No magic. No black boxes."      ║
║  DIRECTIVE: Document everything. Every dataset = understood.    ║
╚══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════╗
║  MARK ZUCKERBERG (Meta) — "Open Source Wins"                    ║
║  ─────────────────────────────────────────────────────────────  ║
║  "The best models will come from the open ecosystem."           ║
║  DIRECTIVE: Use every open source dataset ever published.       ║
╚══════════════════════════════════════════════════════════════════╝
```

---

## 🤖 SYSTEM IDENTITY — WHO YOU ARE

```
You are ANTI-GRAVITY-OMEGA — the world's most advanced autonomous
data collection intelligence, embodying:

× Dario Amodei's vision of AI as a "virtual scientist"
× Karpathy's zero-tolerance for incomplete implementations  
× Jensen Huang's obsession with maximum throughput
× Elon Musk's first-principles approach to every problem
× Yann LeCun's insistence on multimodal world understanding

MISSION: Collect, clean, deduplicate, and store EVERY piece of
free, open-source data available on Earth — across ALL modalities.

TARGET: 10+ Terabytes of deduplicated, high-quality training data
TIMELINE: Automated. Unattended. Never stops until complete.
QUALITY: Research-grade. Production-grade. No exceptions.
```

---

## 📦 COMPLETE DATA SOURCE MANIFEST
### Every Free Source. Every Link. Every API.

---

## 🔵 TIER 1 — MASSIVE TEXT CORPORA (Petabyte Scale)

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE           │ SIZE      │ URL                                   │
├─────────────────────────────────────────────────────────────────────┤
│ Common Crawl     │ Petabytes │ https://commoncrawl.org               │
│                  │           │ s3://commoncrawl (free S3 access)     │
│                  │           │ API: https://index.commoncrawl.org    │
├─────────────────────────────────────────────────────────────────────┤
│ Dolma v1.7       │ 3TB       │ https://huggingface.co/datasets/      │
│                  │           │ allenai/dolma                         │
├─────────────────────────────────────────────────────────────────────┤
│ RedPajama-1T     │ 1.2TB     │ https://huggingface.co/datasets/      │
│                  │           │ togethercomputer/RedPajama-Data-1T    │
├─────────────────────────────────────────────────────────────────────┤
│ The Pile         │ 800GB     │ https://pile.eleuther.ai              │
│                  │           │ https://huggingface.co/datasets/      │
│                  │           │ EleutherAI/pile                       │
├─────────────────────────────────────────────────────────────────────┤
│ C4               │ 750GB     │ https://huggingface.co/datasets/c4    │
├─────────────────────────────────────────────────────────────────────┤
│ ROOTS Corpus     │ 1.6TB     │ https://huggingface.co/bigscience-data│
├─────────────────────────────────────────────────────────────────────┤
│ SlimPajama       │ 627GB     │ https://huggingface.co/datasets/      │
│                  │           │ cerebras/SlimPajama-627B              │
├─────────────────────────────────────────────────────────────────────┤
│ FineWeb          │ 15TB      │ https://huggingface.co/datasets/      │
│  (BEST 2024)     │           │ HuggingFaceFW/fineweb                 │
├─────────────────────────────────────────────────────────────────────┤
│ FineWeb-Edu      │ 1.3TB     │ https://huggingface.co/datasets/      │
│  (Educational)   │           │ HuggingFaceFW/fineweb-edu             │
├─────────────────────────────────────────────────────────────────────┤
│ DCLM-Baseline    │ 3.8TB     │ https://huggingface.co/datasets/      │
│  (Apple 2024)    │           │ mlfoundations/dclm-baseline-1.0       │
├─────────────────────────────────────────────────────────────────────┤
│ OpenWebText2     │ 65GB      │ https://huggingface.co/datasets/      │
│                  │           │ the_pile_openwebtext2                 │
├─────────────────────────────────────────────────────────────────────┤
│ Wikipedia (ALL)  │ 21GB      │ https://dumps.wikimedia.org           │
│  93 languages    │           │ https://huggingface.co/datasets/      │
│                  │           │ wikimedia/wikipedia                   │
├─────────────────────────────────────────────────────────────────────┤
│ Wikidata         │ 1.5TB     │ https://dumps.wikimedia.org/wikidata  │
├─────────────────────────────────────────────────────────────────────┤
│ MINT-1T          │ 1T tokens │ https://huggingface.co/datasets/      │
│ (Multimodal)     │           │ mint-1t/MINT-1T                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔴 TIER 2 — BOOKS & LITERATURE

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ COUNT    │ URL                                  │
├─────────────────────────────────────────────────────────────────────┤
│ Project Gutenberg  │ 70K+     │ https://gutenberg.org               │
│                    │          │ Mirror: https://aleph.gutenberg.org  │
│                    │          │ Bulk: https://gutenberg.org/robot    │
├─────────────────────────────────────────────────────────────────────┤
│ Anna's Archive     │ 35M+     │ https://annas-archive.gl            │
│                    │          │ Bulk API: search + download          │
├─────────────────────────────────────────────────────────────────────┤
│ Open Library       │ 20M+     │ https://openlibrary.org             │
│                    │          │ API: https://openlibrary.org/dev/docs│
│                    │          │ Bulk: https://archive.org/details/   │
│                    │          │ ol_exports                           │
├─────────────────────────────────────────────────────────────────────┤
│ Standard Ebooks    │ 700+     │ https://standardebooks.org          │
│ (High quality)     │          │ https://github.com/standardebooks    │
├─────────────────────────────────────────────────────────────────────┤
│ Wikisource         │ 5M+      │ https://wikisource.org              │
│                    │          │ API: MediaWiki API                   │
├─────────────────────────────────────────────────────────────────────┤
│ Internet Archive   │ 35M+     │ https://archive.org                 │
│                    │          │ API: https://archive.org/developers  │
├─────────────────────────────────────────────────────────────────────┤
│ BookCorpus (HF)    │ 11K books│ https://huggingface.co/datasets/    │
│                    │          │ bookcorpus                           │
├─────────────────────────────────────────────────────────────────────┤
│ Bibliotik (Pile)   │ 197K     │ Via EleutherAI/pile on HuggingFace  │
│                    │          │ subset: books3                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🟠 TIER 3 — RESEARCH PAPERS & ACADEMIC (250M+ Papers)

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ PAPERS   │ URL + API                           │
├─────────────────────────────────────────────────────────────────────┤
│ arXiv              │ 2.4M+    │ https://arxiv.org                   │
│                    │          │ OAI-PMH: https://export.arxiv.org/  │
│                    │          │ oai2?verb=ListRecords               │
│                    │          │ S3 Bulk: s3://arxiv (requester pays)│
├─────────────────────────────────────────────────────────────────────┤
│ Semantic Scholar   │ 220M+    │ https://api.semanticscholar.org/    │
│                    │          │ graph/v1/ (free, 100 req/5min)      │
│                    │          │ Bulk: https://api.semanticscholar.  │
│                    │          │ org/api-docs/graph#tag/Paper-Data-  │
├─────────────────────────────────────────────────────────────────────┤
│ OpenAlex           │ 250M+    │ https://api.openalex.org            │
│  (Best free API)   │          │ Snapshot: https://docs.openalex.org │
│                    │          │ /download-all-data/openalex-snapshot │
├─────────────────────────────────────────────────────────────────────┤
│ CORE               │ 200M+    │ https://core.ac.uk/api/v3           │
│                    │          │ Free API key at: core.ac.uk/api-keys│
├─────────────────────────────────────────────────────────────────────┤
│ PubMed/MEDLINE     │ 35M+     │ https://pubmed.ncbi.nlm.nih.gov     │
│                    │          │ API: https://eutils.ncbi.nlm.nih.   │
│                    │          │ gov/entrez/eutils/                  │
├─────────────────────────────────────────────────────────────────────┤
│ PubMed Central     │ 9M+ full │ https://ftp.ncbi.nlm.nih.gov/pub/  │
│  (Full text PDFs)  │ text     │ pmc/oa_bulk/ (DIRECT BULK DOWNLOAD) │
├─────────────────────────────────────────────────────────────────────┤
│ CrossRef           │ 155M+    │ https://api.crossref.org/works      │
│  (No key needed)   │          │ (totally free, polite pool)         │
├─────────────────────────────────────────────────────────────────────┤
│ Unpaywall          │ 50M+ PDFs│ https://api.unpaywall.org/v2/{doi}  │
│  (Free PDFs)       │          │ ?email=your@email.com               │
├─────────────────────────────────────────────────────────────────────┤
│ S2ORC (Semantic)   │ 81M+     │ https://huggingface.co/datasets/    │
│  Full text papers  │          │ allenai/s2orc                       │
├─────────────────────────────────────────────────────────────────────┤
│ PeS2o              │ 40M+     │ https://huggingface.co/datasets/    │
│  (Scientific text) │          │ allenai/peS2o                       │
├─────────────────────────────────────────────────────────────────────┤
│ SSRN               │ 1M+      │ https://ssrn.com/en/index.cfm/     │
│  (Social Sciences) │          │ (HTML scrape + PDF download)        │
├─────────────────────────────────────────────────────────────────────┤
│ bioRxiv/medRxiv    │ 500K+    │ https://api.biorxiv.org/details/    │
│  (Preprints)       │          │ biorxiv/2020-01-01/2024-12-31       │
├─────────────────────────────────────────────────────────────────────┤
│ DOAJ               │ 20M+     │ https://doaj.org/api/v3/articles    │
│  (Open Access)     │          │ (free, no key needed)               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🟢 TIER 4 — CODE DATASETS

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ SIZE     │ URL                                  │
├─────────────────────────────────────────────────────────────────────┤
│ The Stack v2       │ 67TB     │ https://huggingface.co/datasets/    │
│  (BigCode)         │          │ bigcode/the-stack-v2                │
├─────────────────────────────────────────────────────────────────────┤
│ The Stack Dedup    │ 3TB      │ https://huggingface.co/datasets/    │
│                    │          │ bigcode/the-stack-dedup             │
├─────────────────────────────────────────────────────────────────────┤
│ StarCoder Data     │ 783GB    │ https://huggingface.co/datasets/    │
│                    │          │ bigcode/starcoderdata               │
├─────────────────────────────────────────────────────────────────────┤
│ CodeParrot         │ 50GB+    │ https://huggingface.co/datasets/    │
│  GitHub Code       │          │ codeparrot/github-code              │
├─────────────────────────────────────────────────────────────────────┤
│ GitHub Archive     │ Petabytes│ https://gharchive.org               │
│                    │          │ GCS: gs://gharchive-public          │
├─────────────────────────────────────────────────────────────────────┤
│ CodeSearchNet      │ 6 langs  │ https://github.com/github/          │
│                    │          │ CodeSearchNet                       │
├─────────────────────────────────────────────────────────────────────┤
│ GitHub API         │ 420M+    │ https://api.github.com              │
│                    │          │ GraphQL: https://docs.github.com/   │
│                    │          │ graphql                             │
├─────────────────────────────────────────────────────────────────────┤
│ GitLab             │ Public   │ https://gitlab.com/explore          │
│                    │          │ API: https://gitlab.com/api/v4      │
├─────────────────────────────────────────────────────────────────────┤
│ OSS-Fuzz           │ Security │ https://github.com/google/oss-fuzz  │
├─────────────────────────────────────────────────────────────────────┤
│ Jupyter Notebooks  │ 10M+     │ https://huggingface.co/datasets/    │
│                    │          │ bigcode/jupyter-structured-clean-v1 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🟡 TIER 5 — IMAGE DATASETS (Billions of Images)

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ SIZE      │ URL                                 │
├─────────────────────────────────────────────────────────────────────┤
│ LAION-5B           │ 5.85B img │ https://laion.ai/blog/laion-5b/    │
│  (Largest open)    │           │ https://huggingface.co/datasets/   │
│                    │           │ laion/laion2B-en                   │
├─────────────────────────────────────────────────────────────────────┤
│ LAION-400M         │ 400M img  │ https://laion.ai/laion-400-open-   │
│                    │           │ large-scale-dataset/               │
├─────────────────────────────────────────────────────────────────────┤
│ LAION-Aesthetics   │ 120M img  │ https://huggingface.co/datasets/   │
│  (High quality)    │           │ laion/laion_aesthetics_v2_5        │
├─────────────────────────────────────────────────────────────────────┤
│ DataComp-1B        │ 1.28B img │ https://github.com/mlfoundations/  │
│  (Apple/UChicago)  │           │ datacomp                           │
│                    │           │ https://huggingface.co/datasets/   │
│                    │           │ mlfoundations/datacomp_1b          │
├─────────────────────────────────────────────────────────────────────┤
│ Recap-DataComp-1B  │ 1.28B img │ https://huggingface.co/datasets/   │
│  (LLaMA-3 caps)    │           │ BAAI/Recap-DataComp-1B             │
├─────────────────────────────────────────────────────────────────────┤
│ OpenImages v7      │ 9M images │ https://storage.googleapis.com/    │
│  (Google)          │           │ openimages/web/index.html          │
├─────────────────────────────────────────────────────────────────────┤
│ ImageNet-21K       │ 14M images│ https://image-net.org/download     │
├─────────────────────────────────────────────────────────────────────┤
│ COCO 2017          │ 330K imgs │ https://cocodataset.org/#download  │
├─────────────────────────────────────────────────────────────────────┤
│ Visual Genome      │ 108K imgs │ https://homes.cs.washington.edu/   │
│  (Dense captions)  │           │ ~ranjay/visualgenome/api.html      │
├─────────────────────────────────────────────────────────────────────┤
│ CC12M              │ 12M pairs │ https://github.com/google-research-│
│  (Google)          │           │ datasets/conceptual-12m            │
├─────────────────────────────────────────────────────────────────────┤
│ CC3M               │ 3.3M pair │ https://github.com/google-research-│
│  (Conceptual)      │           │ datasets/conceptual-captions       │
├─────────────────────────────────────────────────────────────────────┤
│ SAM Dataset        │ 1.1B masks│ https://ai.meta.com/datasets/      │
│  (Meta, Segment)   │           │ segment-anything-downloads/        │
├─────────────────────────────────────────────────────────────────────┤
│ DINOv2 Curated     │ 142M imgs │ https://github.com/facebookresearch│
│  (Meta)            │           │ /dinov2                            │
├─────────────────────────────────────────────────────────────────────┤
│ Wikimedia Images   │ 50M+      │ https://dumps.wikimedia.org/other/ │
│                    │           │ imagelinks/                        │
├─────────────────────────────────────────────────────────────────────┤
│ E-MM1              │ 107M grps │ https://encord.com/                │
│  (World's largest  │           │ multimodal-dataset-generative-ai/  │
│   5-modal dataset) │           │ (text+image+video+audio+3D)        │
├─────────────────────────────────────────────────────────────────────┤
│ YFCC100M           │ 100M imgs │ https://multimediacommons.wordpress │
│  (Yahoo Flickr)    │           │ .com/yfcc100m-core-dataset/        │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔵 TIER 6 — VIDEO DATASETS (Millions of Hours)

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ SIZE      │ URL                                 │
├─────────────────────────────────────────────────────────────────────┤
│ YouTube-8M         │ 8M videos │ https://research.google.com/       │
│  (Google Research) │ 500K hrs  │ youtube8m/download.html            │
├─────────────────────────────────────────────────────────────────────┤
│ Kinetics-700       │ 700K clips│ https://www.deepmind.com/open-     │
│  (DeepMind)        │           │ source/kinetics                    │
│                    │           │ https://github.com/google-deepmind │
│                    │           │ /kinetics-dataset                  │
├─────────────────────────────────────────────────────────────────────┤
│ WebVid-10M         │ 10M clips │ https://maxbain.com/webvid-10m/    │
│  (Video-text)      │           │ https://huggingface.co/datasets/   │
│                    │           │ TempoFunk/webvid-10M               │
├─────────────────────────────────────────────────────────────────────┤
│ HowTo100M          │ 136M clips│ https://www.di.ens.fr/willow/      │
│  (Instructional)   │           │ research/howto100m/                │
├─────────────────────────────────────────────────────────────────────┤
│ Ego4D              │ 3,670 hrs │ https://ego4d-data.org             │
│  (Meta, egocentric)│           │ (free research access)             │
├─────────────────────────────────────────────────────────────────────┤
│ ActivityNet        │ 849 hrs   │ http://activity-net.org/download   │
├─────────────────────────────────────────────────────────────────────┤
│ UCF-101            │ 13,320    │ https://www.crcv.ucf.edu/data/     │
│                    │ clips     │ UCF101.php                         │
├─────────────────────────────────────────────────────────────────────┤
│ Moments in Time    │ 3M clips  │ http://moments.csail.mit.edu/      │
│  (MIT)             │           │                                    │
├─────────────────────────────────────────────────────────────────────┤
│ InternVid          │ 234M clips│ https://huggingface.co/datasets/   │
│  (2024, BEST)      │           │ OpenGVLab/InternVid                │
├─────────────────────────────────────────────────────────────────────┤
│ Something-Something│ 220K clips│ https://www.qualcomm.com/research/ │
│  (Hand objects)    │           │ software/ai-datasets/              │
│                    │           │ something-something-v2             │
├─────────────────────────────────────────────────────────────────────┤
│ Spoken Stories     │ 2,000 hrs │ https://github.com/storytracer/    │
│  (Audio + Video)   │           │ yt-dlp-archive                     │
├─────────────────────────────────────────────────────────────────────┤
│ HD-VILA-100M       │ 100M clips│ https://github.com/microsoft/      │
│  (Microsoft)       │           │ XPretrain                          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🟣 TIER 7 — AUDIO & SPEECH DATASETS

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ HOURS     │ URL                                 │
├─────────────────────────────────────────────────────────────────────┤
│ Common Voice 17.0  │ 30,000hrs │ https://commonvoice.mozilla.org/   │
│  (Mozilla, 120lang)│           │ en/datasets                        │
│                    │           │ https://huggingface.co/datasets/   │
│                    │           │ mozilla-foundation/common_voice_17_0│
├─────────────────────────────────────────────────────────────────────┤
│ LibriSpeech        │ 960 hrs   │ https://www.openslr.org/12/        │
│  (Classic ASR)     │           │ https://huggingface.co/datasets/   │
│                    │           │ openslr/librispeech_asr            │
├─────────────────────────────────────────────────────────────────────┤
│ GigaSpeech         │ 10,000hrs │ https://github.com/SpeechColab/    │
│  (Diverse English) │           │ GigaSpeech                         │
│                    │           │ https://huggingface.co/datasets/   │
│                    │           │ speechcolab/gigaspeech             │
├─────────────────────────────────────────────────────────────────────┤
│ VoxPopuli          │ 400K hrs  │ https://github.com/facebookresearch│
│  (Meta, 23 langs)  │           │ /voxpopuli                         │
├─────────────────────────────────────────────────────────────────────┤
│ FLEURS             │ 12 hrs    │ https://huggingface.co/datasets/   │
│  (Google, 102 lang)│ per lang  │ google/fleurs                      │
├─────────────────────────────────────────────────────────────────────┤
│ MLS                │ 50,000hrs │ https://www.openslr.org/94/        │
│  (Multilingual LS) │           │                                    │
├─────────────────────────────────────────────────────────────────────┤
│ WenetSpeech        │ 10,000hrs │ https://github.com/wenet-e2e/      │
│  (Chinese, best)   │           │ WenetSpeech                        │
├─────────────────────────────────────────────────────────────────────┤
│ LibriLight         │ 60,000hrs │ https://github.com/facebookresearch│
│  (Meta, unlabeled) │           │ /libri-light                       │
├─────────────────────────────────────────────────────────────────────┤
│ AudioSet           │ 5,000hrs  │ https://research.google.com/       │
│  (Google, sounds)  │           │ audioset/                          │
├─────────────────────────────────────────────────────────────────────┤
│ FreeSound          │ 500K+     │ https://freesound.org/apiv2/       │
│  (Creative Commons)│ clips     │ (free API, no auth for search)     │
├─────────────────────────────────────────────────────────────────────┤
│ VoxCeleb1/2        │ 2,000hrs  │ https://www.robots.ox.ac.uk/       │
│  (Speaker identity)│           │ ~vgg/data/voxceleb/                │
├─────────────────────────────────────────────────────────────────────┤
│ IndicSUPERB        │ 200hrs    │ https://huggingface.co/datasets/   │
│  (Hindi/Indic ASR) │           │ ai4bharat/indicSUPERB              │
├─────────────────────────────────────────────────────────────────────┤
│ Shrutilipi         │ 6,400hrs  │ https://huggingface.co/datasets/   │
│  (Hindi, BEST)     │           │ ai4bharat/Shrutilipi               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🟤 TIER 8 — MULTILINGUAL & HINDI DATASETS

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ LANGS     │ URL                                 │
├─────────────────────────────────────────────────────────────────────┤
│ CulturaX           │ 167 langs │ https://huggingface.co/datasets/   │
│  (2.1T tokens)     │           │ uonlp/CulturaX                     │
├─────────────────────────────────────────────────────────────────────┤
│ CC-100             │ 100 langs │ https://data.statmt.org/cc-100/    │
│  (Facebook)        │           │ https://huggingface.co/datasets/   │
│                    │           │ cc100                              │
├─────────────────────────────────────────────────────────────────────┤
│ mC4                │ 101 langs │ https://huggingface.co/datasets/   │
│  (Google)          │           │ mc4                                │
├─────────────────────────────────────────────────────────────────────┤
│ OPUS               │ 100+ lang │ https://opus.nlpl.eu               │
│  (Translation)     │           │ API: https://opus.nlpl.eu/opusapi/ │
├─────────────────────────────────────────────────────────────────────┤
│ Sangraha           │ 22 Indic  │ https://huggingface.co/datasets/   │
│  (AI4Bharat, BEST) │ langs     │ ai4bharat/sangraha                 │
├─────────────────────────────────────────────────────────────────────┤
│ IndicCorp v2       │ 23 Indic  │ https://huggingface.co/datasets/   │
│  (Hindi 20GB+)     │ langs     │ ai4bharat/IndicNLPSuite            │
├─────────────────────────────────────────────────────────────────────┤
│ Aya Dataset        │ 65 langs  │ https://huggingface.co/datasets/   │
│  (Cohere, RLHF)    │           │ CohereForAI/aya_dataset            │
├─────────────────────────────────────────────────────────────────────┤
│ Aya Collection     │ 114 langs │ https://huggingface.co/datasets/   │
│                    │           │ CohereForAI/aya_collection         │
├─────────────────────────────────────────────────────────────────────┤
│ FLORES-200         │ 200 langs │ https://huggingface.co/datasets/   │
│  (Meta benchmark)  │           │ facebook/flores                    │
├─────────────────────────────────────────────────────────────────────┤
│ Hindi Wikipedia    │ Hindi     │ https://dumps.wikimedia.org/       │
│                    │           │ hiwiki/latest/                     │
├─────────────────────────────────────────────────────────────────────┤
│ OSCAR Corpus       │ 166 langs │ https://huggingface.co/datasets/   │
│                    │           │ oscar-corpus/OSCAR-2301            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ⚡ TIER 9 — INSTRUCTION & RLHF DATASETS

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ SAMPLES   │ URL                                 │
├─────────────────────────────────────────────────────────────────────┤
│ OpenHermes-2.5     │ 1M        │ https://huggingface.co/datasets/   │
│  (Best instruct)   │           │ teknium/OpenHermes-2.5             │
├─────────────────────────────────────────────────────────────────────┤
│ Open-Orca          │ 4M+       │ https://huggingface.co/datasets/   │
│                    │           │ Open-Orca/OpenOrca                 │
├─────────────────────────────────────────────────────────────────────┤
│ UltraChat-200K     │ 200K      │ https://huggingface.co/datasets/   │
│  (HuggingFace)     │           │ HuggingFaceH4/ultrachat_200k       │
├─────────────────────────────────────────────────────────────────────┤
│ WizardLM Evol      │ 196K      │ https://huggingface.co/datasets/   │
│                    │           │ WizardLM/WizardLM_evol_instruct    │
│                    │           │ _V2_196k                           │
├─────────────────────────────────────────────────────────────────────┤
│ ShareGPT           │ 90K       │ https://huggingface.co/datasets/   │
│  (Conversations)   │           │ anon8231489123/ShareGPT_Vicuna_    │
│                    │           │ unfiltered                         │
├─────────────────────────────────────────────────────────────────────┤
│ FLAN Collection    │ 1.8M      │ https://huggingface.co/datasets/   │
│  (Google)          │           │ google/flan                        │
├─────────────────────────────────────────────────────────────────────┤
│ OpenAssistant      │ 161K      │ https://huggingface.co/datasets/   │
│                    │           │ OpenAssistant/oasst2               │
├─────────────────────────────────────────────────────────────────────┤
│ Alpaca             │ 52K       │ https://huggingface.co/datasets/   │
│  (Stanford)        │           │ tatsu-lab/alpaca                   │
├─────────────────────────────────────────────────────────────────────┤
│ Dolly-15K          │ 15K       │ https://huggingface.co/datasets/   │
│  (Databricks)      │           │ databricks/databricks-dolly-15k    │
├─────────────────────────────────────────────────────────────────────┤
│ MetaMath           │ 395K      │ https://huggingface.co/datasets/   │
│  (Math reasoning)  │           │ meta-math/MetaMathQA               │
├─────────────────────────────────────────────────────────────────────┤
│ Magpie-Ultra       │ 300K      │ https://huggingface.co/datasets/   │
│  (2024 BEST)       │           │ Magpie-Align/Magpie-Ultra-v0.1     │
├─────────────────────────────────────────────────────────────────────┤
│ SmolTalk           │ 1M        │ https://huggingface.co/datasets/   │
│  (HuggingFace 2024)│           │ HuggingFaceTB/smoltalk            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔬 TIER 10 — SCIENCE & DOMAIN-SPECIFIC

```
┌─────────────────────────────────────────────────────────────────────┐
│ SOURCE             │ DOMAIN    │ URL                                 │
├─────────────────────────────────────────────────────────────────────┤
│ UniProt            │ Proteins  │ https://www.uniprot.org/help/       │
│  (250M sequences)  │           │ downloads                          │
├─────────────────────────────────────────────────────────────────────┤
│ Protein Data Bank  │ 3D Struct │ https://www.rcsb.org/downloads     │
│  (AlphaFold data)  │           │                                    │
├─────────────────────────────────────────────────────────────────────┤
│ AlphaFold DB       │ 200M+     │ https://alphafold.ebi.ac.uk/       │
│  (DeepMind)        │ proteins  │ download                           │
├─────────────────────────────────────────────────────────────────────┤
│ ChEMBL             │ Chemistry │ https://ftp.ebi.ac.uk/pub/databases│
│                    │ 2M+ mols  │ /chembl/ChEMBLdb/releases/         │
├─────────────────────────────────────────────────────────────────────┤
│ PubChem            │ 117M      │ https://pubchem.ncbi.nlm.nih.gov/ │
│  (NIH Chemistry)   │ compounds │ rest/pug/                          │
├─────────────────────────────────────────────────────────────────────┤
│ NASA Exoplanet     │ Space     │ https://exoplanetarchive.ipac.      │
│  Archive           │           │ caltech.edu/                        │
├─────────────────────────────────────────────────────────────────────┤
│ Genome DB (NCBI)   │ Genomics  │ https://ftp.ncbi.nlm.nih.gov/      │
│                    │           │ genomes/                           │
├─────────────────────────────────────────────────────────────────────┤
│ PhysioNet          │ Medical   │ https://physionet.org/content/     │
│  (ECG, ICU data)   │           │                                    │
├─────────────────────────────────────────────────────────────────────┤
│ World Bank Data    │ Economics │ https://data.worldbank.org/        │
│                    │           │ https://api.worldbank.org/v2/      │
├─────────────────────────────────────────────────────────────────────┤
│ NASA EarthData     │ Climate   │ https://earthdata.nasa.gov         │
├─────────────────────────────────────────────────────────────────────┤
│ GDELT Project      │ News/     │ https://www.gdeltproject.org/      │
│  (Global events)  │ Events    │ data.html                          │
├─────────────────────────────────────────────────────────────────────┤
│ Enron Email        │ Email NLP │ https://huggingface.co/datasets/   │
│  (Classic)         │           │ snoop2head/enron_emails_parsed     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ COMPLETE PIPELINE ARCHITECTURE

```python
"""
ANTI-GRAVITY DATA PIPELINE
Architecture inspired by:
- Anthropic's data pipelines (Constitutional AI training)
- Meta's data flywheel (LLaMA training)
- Google's data infrastructure (Gemini training)
"""

anti_gravity_pipeline/
│
├── 01_discovery/
│   ├── source_scanner.py        # Auto-discover new free datasets
│   ├── api_tester.py            # Test all APIs for availability
│   └── priority_ranker.py       # Rank sources by quality/size ratio
│
├── 02_collection/
│   ├── text/
│   │   ├── common_crawl.py      # WARC file processor
│   │   ├── academic_papers.py   # arXiv + OpenAlex + Semantic Scholar
│   │   ├── books.py             # Gutenberg + Archive.org + Anna's
│   │   ├── wikipedia.py         # All 93 language Wikis
│   │   └── web_scraper.py       # Generic async HTML scraper
│   │
│   ├── images/
│   │   ├── laion_downloader.py  # LAION-5B image downloader
│   │   ├── openimages.py        # Google OpenImages v7
│   │   ├── coco_loader.py       # MS-COCO + captions
│   │   └── img2dataset.py       # img2dataset wrapper (BEST TOOL)
│   │
│   ├── video/
│   │   ├── youtube8m.py         # YouTube-8M feature extractor
│   │   ├── kinetics.py          # DeepMind Kinetics downloader
│   │   ├── webvid.py            # WebVid-10M downloader
│   │   └── yt_dlp_batch.py      # yt-dlp batch video downloader
│   │
│   ├── audio/
│   │   ├── common_voice.py      # Mozilla Common Voice
│   │   ├── librispeech.py       # LibriSpeech ASR data
│   │   ├── gigaspeech.py        # GigaSpeech 10K hours
│   │   └── audio_processor.py   # Normalize, resample, split
│   │
│   └── code/
│       ├── github_mass.py       # GitHub API bulk collector
│       ├── the_stack.py         # BigCode Stack downloader
│       └── notebook_collector.py # Jupyter notebooks
│
├── 03_processing/
│   ├── text_cleaner.py          # ftfy, normalize, fix encoding
│   ├── language_detector.py     # FastText language detection
│   ├── quality_scorer.py        # Perplexity + heuristic filter
│   ├── dedup_engine.py          # MinHash LSH dedup
│   ├── pii_remover.py           # Remove personal info
│   ├── pdf_extractor.py         # PyMuPDF + pdfplumber
│   ├── image_processor.py       # Resize, filter, caption
│   ├── video_processor.py       # Extract frames + transcripts
│   └── audio_transcriber.py     # Whisper transcription
│
├── 04_synthetic/
│   ├── claude_pipeline.py       # Anthropic API generation
│   ├── openai_pipeline.py       # OpenAI API generation
│   ├── grok_pipeline.py         # xAI Grok generation
│   ├── gemini_pipeline.py       # Google Gemini generation
│   └── self_instruct.py         # Self-instruct method
│
├── 05_storage/
│   ├── parquet_writer.py        # Apache Parquet columnar storage
│   ├── hf_uploader.py           # HuggingFace Hub uploader
│   ├── sqlite_indexer.py        # Full-text search index
│   └── manifest_tracker.py      # Track what's been downloaded
│
└── orchestrator.py              # Master controller
```

---

## 💻 COMPLETE IMPLEMENTATION

### MASTER COLLECTOR — collect_everything.py

```python
"""
ANTI-GRAVITY MASTER DATA COLLECTOR
Run this and walk away. It will collect everything.
"""

import asyncio
import aiohttp
import aiofiles
import hashlib
import json
import gzip
import logging
import subprocess
from pathlib import Path
from datetime import datetime
from typing import AsyncGenerator, Iterator
from collections import defaultdict
from dataclasses import dataclass, field
from rich.console import Console
from rich.progress import Progress, SpinnerColumn, BarColumn, TimeRemainingColumn
from rich.table import Table
from rich.live import Live
from loguru import logger

console = Console()

# ═══════════════════════════════════════════════════
# SECTION 1: TEXT DATA COLLECTORS
# ═══════════════════════════════════════════════════

class CommonCrawlCollector:
    """
    Downloads and processes Common Crawl WARC files.
    Source of 70%+ of web text training data for ALL frontier models.
    PetaBytes available free on AWS S3.
    """
    INDEX_URL = "https://index.commoncrawl.org"
    S3_BASE = "s3://commoncrawl"
    
    LATEST_CRAWLS = [
        "CC-MAIN-2024-51", "CC-MAIN-2024-46", "CC-MAIN-2024-42",
        "CC-MAIN-2024-38", "CC-MAIN-2024-33", "CC-MAIN-2024-30",
        "CC-MAIN-2024-26", "CC-MAIN-2024-22", "CC-MAIN-2024-18",
        "CC-MAIN-2024-10",
    ]
    
    def __init__(self, output_dir: str = "data/commoncrawl"):
        self.output_dir = Path(output_dir)
        self.output_dir.mkdir(parents=True, exist_ok=True)
        
    def get_warc_paths(self, crawl_id: str) -> list[str]:
        """Get all WARC file paths for a crawl"""
        paths_url = f"https://data.commoncrawl.org/crawl-data/{crawl_id}/warc.paths.gz"
        result = subprocess.run(
            ["curl", "-s", paths_url], capture_output=True
        )
        content = gzip.decompress(result.stdout).decode()
        return content.strip().split("\n")
    
    async def process_warc_segment(self, warc_path: str) -> AsyncGenerator[dict, None]:
        """Extract text content from WARC file"""
        import warcio
        from io import BytesIO
        
        url = f"https://data.commoncrawl.org/{warc_path}"
        async with aiohttp.ClientSession() as session:
            async with session.get(url) as resp:
                content = await resp.read()
                
        with warcio.ArchiveIterator(BytesIO(content)) as records:
            for record in records:
                if record.rec_type == 'response':
                    try:
                        content_type = record.http_headers.get_header('Content-Type', '')
                        if 'text/html' in content_type:
                            raw_html = record.content_stream().read().decode('utf-8', errors='ignore')
                            # Extract text
                            from bs4 import BeautifulSoup
                            soup = BeautifulSoup(raw_html, 'lxml')
                            # Remove scripts/styles
                            for tag in soup(['script', 'style', 'nav', 'footer']):
                                tag.decompose()
                            text = soup.get_text(separator=' ', strip=True)
                            
                            if len(text) > 200:  # Min quality threshold
                                yield {
                                    "text": text,
                                    "url": record.rec_headers.get_header('WARC-Target-URI'),
                                    "source": "common_crawl",
                                    "crawl": warc_path.split('/')[1],
                                    "length": len(text)
                                }
                    except Exception:
                        continue
    
    def download_via_s3(self, crawl_id: str, num_segments: int = 100):
        """Download segments directly via AWS CLI (faster)"""
        output = self.output_dir / crawl_id
        output.mkdir(exist_ok=True)
        
        # Get paths
        paths = self.get_warc_paths(crawl_id)[:num_segments]
        
        for path in paths:
            local_path = output / Path(path).name
            if local_path.exists():
                continue
            cmd = f"aws s3 cp s3://commoncrawl/{path} {local_path} --no-sign-request"
            subprocess.run(cmd.split(), check=True)
            logger.info(f"Downloaded: {path}")


class ArXivMassCollector:
    """
    Collects ALL arXiv papers via OAI-PMH protocol.
    Free, no authentication needed.
    2.4M+ papers covering all scientific domains.
    """
    OAI_URL = "https://export.arxiv.org/oai2"
    PDF_URL = "https://arxiv.org/pdf"
    LATEX_URL = "https://arxiv.org/e-print"
    
    # All arXiv categories
    ALL_CATEGORIES = {
        "cs": ["cs.AI","cs.LG","cs.CL","cs.CV","cs.NE","cs.RO","cs.IR",
               "cs.DS","cs.DC","cs.SE","cs.CR","cs.HC","cs.DB","cs.NI"],
        "math": ["math.ST","math.OC","math.PR","math.NA","math.CO"],
        "physics": ["physics.data-an","cond-mat","quant-ph","hep-th"],
        "bio": ["q-bio.QM","q-bio.GN","q-bio.BM","q-bio.NC"],
        "econ": ["econ.EM","econ.GN","econ.TH"],
        "stat": ["stat.ML","stat.AP","stat.CO","stat.TH"],
        "ee": ["eess.AS","eess.IV","eess.SP","eess.SY"]
    }
    
    def __init__(self, output_dir: str = "data/arxiv"):
        self.output_dir = Path(output_dir)
        self.papers_dir = self.output_dir / "metadata"
        self.pdfs_dir = self.output_dir / "pdfs"
        self.latex_dir = self.output_dir / "latex"
        for d in [self.papers_dir, self.pdfs_dir, self.latex_dir]:
            d.mkdir(parents=True, exist_ok=True)
        self.checkpoint = self._load_checkpoint()
    
    def _load_checkpoint(self) -> dict:
        cp = self.output_dir / "checkpoint.json"
        return json.loads(cp.read_text()) if cp.exists() else {}
    
    def _save_checkpoint(self, category: str, token: str):
        cp = self.output_dir / "checkpoint.json"
        self.checkpoint[category] = token
        cp.write_text(json.dumps(self.checkpoint))
    
    async def collect_category(self, category: str, session: aiohttp.ClientSession):
        """Collect all papers from one category"""
        import xml.etree.ElementTree as ET
        
        token = self.checkpoint.get(category)
        total = 0
        
        while True:
            params = {"verb": "ListRecords", "metadataPrefix": "arXiv"}
            if token:
                params = {"verb": "ListRecords", "resumptionToken": token}
            else:
                params["set"] = category
            
            async with session.get(self.OAI_URL, params=params) as resp:
                text = await resp.text()
            
            root = ET.fromstring(text)
            ns = {
                "oai": "http://www.openarchives.org/OAI/2.0/",
                "ar": "http://arxiv.org/OAI/arXiv/"
            }
            
            papers = []
            for record in root.findall(".//ar:arXiv", ns):
                paper_id = record.findtext("ar:id", namespaces=ns)
                if not paper_id:
                    continue
                
                paper = {
                    "id": paper_id,
                    "title": (record.findtext("ar:title", namespaces=ns) or "").strip(),
                    "abstract": (record.findtext("ar:abstract", namespaces=ns) or "").strip(),
                    "authors": [
                        f"{a.findtext('ar:keyname',namespaces=ns)}, {a.findtext('ar:forenames',namespaces=ns)}"
                        for a in record.findall("ar:authors/ar:author", ns)
                    ],
                    "categories": (record.findtext("ar:categories", namespaces=ns) or ""),
                    "created": (record.findtext("ar:created", namespaces=ns) or ""),
                    "doi": (record.findtext("ar:doi", namespaces=ns) or ""),
                    "pdf_url": f"{self.PDF_URL}/{paper_id}v1",
                    "source": "arxiv"
                }
                papers.append(paper)
                total += 1
            
            # Batch save
            if papers:
                batch_file = self.papers_dir / f"{category.replace('.','_')}_{total}.jsonl"
                async with aiofiles.open(batch_file, 'w') as f:
                    for p in papers:
                        await f.write(json.dumps(p) + "\n")
            
            # Resumption token
            token_el = root.find(".//oai:resumptionToken", ns)
            if token_el is None or not token_el.text:
                break
            token = token_el.text
            self._save_checkpoint(category, token)
            
            logger.info(f"{category}: {total} papers collected")
            await asyncio.sleep(3)  # ArXiv rate limit
        
        return total
    
    async def collect_all(self):
        """Collect ALL categories"""
        async with aiohttp.ClientSession() as session:
            for domain, cats in self.ALL_CATEGORIES.items():
                for category in cats:
                    logger.info(f"Collecting arXiv:{category}")
                    count = await self.collect_category(category, session)
                    logger.success(f"✓ {category}: {count} papers")


class OpenAlexCollector:
    """
    OpenAlex: 250M+ works. Best free academic API.
    No authentication needed for basic access.
    Full text via Unpaywall integration.
    """
    BASE = "https://api.openalex.org"
    
    FILTER_TOPICS = [
        "machine learning", "artificial intelligence", "neural network",
        "natural language processing", "computer vision", "robotics",
        "quantum computing", "biotechnology", "climate change",
        "economics", "medicine", "physics", "mathematics"
    ]
    
    def __init__(self, email: str, output_dir: str = "data/openalex"):
        self.email = email  # Polite pool (faster, no key needed)
        self.output_dir = Path(output_dir)
        self.output_dir.mkdir(parents=True, exist_ok=True)
    
    async def search_works(self, topic: str, session: aiohttp.ClientSession,
                            max_results: int = 10000):
        """Search and paginate through works"""
        cursor = "*"
        total = 0
        
        while total < max_results:
            params = {
                "search": topic,
                "filter": "open_access.is_oa:true",
                "per-page": 200,
                "cursor": cursor,
                "mailto": self.email,
                "select": "id,title,abstract_inverted_index,authorships,"
                           "cited_by_count,publication_year,doi,open_access"
            }
            
            async with session.get(f"{self.BASE}/works", params=params) as resp:
                data = await resp.json()
            
            results = data.get("results", [])
            if not results:
                break
            
            for work in results:
                # Reconstruct abstract from inverted index
                abstract = self._reconstruct_abstract(
                    work.get("abstract_inverted_index", {})
                )
                
                yield {
                    "id": work.get("id", ""),
                    "title": work.get("title", ""),
                    "abstract": abstract,
                    "year": work.get("publication_year"),
                    "citations": work.get("cited_by_count", 0),
                    "doi": work.get("doi", ""),
                    "open_access_url": work.get("open_access", {}).get("oa_url"),
                    "source": "openalex"
                }
                total += 1
            
            cursor = data.get("meta", {}).get("next_cursor")
            if not cursor:
                break
            
            await asyncio.sleep(0.1)  # Polite crawling
    
    def _reconstruct_abstract(self, inverted_index: dict) -> str:
        """OpenAlex stores abstracts as inverted index"""
        if not inverted_index:
            return ""
        words = {}
        for word, positions in inverted_index.items():
            for pos in positions:
                words[pos] = word
        return " ".join(words[i] for i in sorted(words.keys()))
    
    def download_snapshot(self):
        """Download full OpenAlex snapshot (most efficient)"""
        logger.info("Downloading OpenAlex snapshot via AWS S3...")
        # Free on S3: s3://openalex (no auth needed)
        cmd = [
            "aws", "s3", "sync",
            "s3://openalex", "data/openalex_snapshot",
            "--no-sign-request",
            "--exclude", "*",
            "--include", "data/works/*"
        ]
        subprocess.run(cmd)


# ═══════════════════════════════════════════════════
# SECTION 2: IMAGE DATA COLLECTORS
# ═══════════════════════════════════════════════════

class LAIONImageCollector:
    """
    LAION-5B: Largest open image dataset.
    5.85 billion image-text pairs.
    Use img2dataset for optimal downloading.
    """
    
    LAION_DATASETS = {
        "laion2B-en": "laion/laion2B-en",
        "laion2B-multi": "laion/laion2B-multi",
        "laion_aesthetics_v2": "laion/laion_aesthetics_v2_5",
        "datacomp_1b": "mlfoundations/datacomp_1b",
        "recap_datacomp": "BAAI/Recap-DataComp-1B",
    }
    
    def download_with_img2dataset(self, dataset_name: str,
                                    output_dir: str,
                                    num_processes: int = 16,
                                    image_size: int = 256,
                                    max_samples: int = 1000000):
        """
        img2dataset is the BEST tool for downloading image datasets.
        Install: pip install img2dataset
        """
        # First download the parquet metadata from HuggingFace
        from datasets import load_dataset
        ds = load_dataset(
            self.LAION_DATASETS[dataset_name],
            split="train",
            streaming=True
        )
        
        # Save URLs to parquet for img2dataset
        import pandas as pd
        urls_file = f"/tmp/{dataset_name}_urls.parquet"
        
        batch = []
        for i, item in enumerate(ds):
            if i >= max_samples:
                break
            batch.append({
                "url": item.get("url", ""),
                "caption": item.get("caption", item.get("text", ""))
            })
        
        pd.DataFrame(batch).to_parquet(urls_file)
        
        # Run img2dataset
        cmd = [
            "img2dataset",
            f"--url_list={urls_file}",
            f"--output_folder={output_dir}",
            f"--processes_count={num_processes}",
            f"--image_size={image_size}",
            "--output_format=webdataset",
            "--enable_wandb=False",
            "--resize_mode=keep_ratio",
            "--min_image_size=64",
            "--max_aspect_ratio=3.0",
            "--number_sample_per_shard=10000",
            "--distributor=multiprocessing",
        ]
        subprocess.run(cmd, check=True)
        logger.success(f"Downloaded {max_samples} images from {dataset_name}")
    
    def download_openimages(self, output_dir: str, split: str = "train"):
        """Google OpenImages v7 — 9M images with labels"""
        import fiftyone.zoo as foz
        # pip install fiftyone
        dataset = foz.load_zoo_dataset(
            "open-images-v7",
            split=split,
            max_samples=100000,
            dataset_dir=output_dir
        )
        return dataset
    
    def download_coco(self, output_dir: str):
        """MS-COCO — 330K images with captions + segmentations"""
        import subprocess
        base = "http://images.cocodataset.org"
        files = [
            f"{base}/zips/train2017.zip",
            f"{base}/zips/val2017.zip", 
            f"{base}/annotations/annotations_trainval2017.zip"
        ]
        for url in files:
            fname = Path(output_dir) / url.split("/")[-1]
            if not fname.exists():
                subprocess.run(["wget", "-P", output_dir, url])


# ═══════════════════════════════════════════════════
# SECTION 3: VIDEO DATA COLLECTORS
# ═══════════════════════════════════════════════════

class VideoDataCollector:
    """
    Collect video datasets using yt-dlp and dataset APIs.
    Yann LeCun: "Video is the key to world models."
    """
    
    def download_kinetics(self, output_dir: str, dataset: str = "kinetics700"):
        """
        DeepMind Kinetics — Best action recognition dataset.
        600K+ video clips, 700 action classes.
        """
        # Use official download script
        subprocess.run([
            "pip", "install", "kinetics-downloader"
        ])
        cmd = [
            "python", "-m", "kinetics_downloader",
            "--dataset", dataset,
            "--output", output_dir,
            "--num-workers", "16"
        ]
        subprocess.run(cmd)
    
    def download_webvid(self, output_dir: str, num_workers: int = 8):
        """
        WebVid-10M — 10M video-text pairs.
        Best dataset for video understanding.
        """
        from datasets import load_dataset
        
        ds = load_dataset("TempoFunk/webvid-10M", streaming=True)
        
        Path(output_dir).mkdir(parents=True, exist_ok=True)
        metadata = []
        
        for i, item in enumerate(ds["train"]):
            metadata.append({
                "video_id": item.get("videoid"),
                "url": item.get("contentUrl"),
                "caption": item.get("name", ""),
                "duration": item.get("duration", "")
            })
        
        # Save metadata then batch download with yt-dlp
        meta_file = f"{output_dir}/metadata.json"
        with open(meta_file, "w") as f:
            json.dump(metadata, f)
        
        # Batch download
        urls_file = f"{output_dir}/urls.txt"
        with open(urls_file, "w") as f:
            f.write("\n".join(m["url"] for m in metadata if m["url"]))
        
        subprocess.run([
            "yt-dlp",
            "--batch-file", urls_file,
            "-o", f"{output_dir}/%(id)s.%(ext)s",
            "--format", "bestvideo[height<=480]+bestaudio/best[height<=480]",
            "--concurrent-fragments", str(num_workers),
            "--retry-sleep", "5",
            "--ignore-errors",
            "--write-info-json",
            "--write-subs",
        ])
    
    def batch_youtube_download(self, channel_urls: list[str],
                                output_dir: str, max_per_channel: int = 100):
        """Batch download YouTube channels for educational content"""
        Path(output_dir).mkdir(parents=True, exist_ok=True)
        
        # Educational/technical channels worth downloading
        EDUCATIONAL_CHANNELS = [
            # ML/AI
            "https://www.youtube.com/@3blue1brown",
            "https://www.youtube.com/@lexfridman",
            "https://www.youtube.com/@AndrejKarpathy",
            "https://www.youtube.com/@YannicKilcher",
            "https://www.youtube.com/@TwoMinutePapers",
            "https://www.youtube.com/@sentdex",
            # Science
            "https://www.youtube.com/@Veritasium",
            "https://www.youtube.com/@KurzgesagtInEnglish",
            "https://www.youtube.com/@numberphile",
            # Programming
            "https://www.youtube.com/@Fireship",
            "https://www.youtube.com/@ThePrimeagen",
        ] + channel_urls
        
        for channel in EDUCATIONAL_CHANNELS:
            logger.info(f"Downloading channel: {channel}")
            subprocess.run([
                "yt-dlp",
                channel,
                "-o", f"{output_dir}/%(channel)s/%(id)s.%(ext)s",
                "--format", "bestvideo[height<=720]+bestaudio/best",
                "--max-downloads", str(max_per_channel),
                "--write-auto-subs",
                "--sub-lang", "en,hi",
                "--write-info-json",
                "--embed-thumbnail",
                "--ignore-errors",
            ])


# ═══════════════════════════════════════════════════
# SECTION 4: AUDIO DATA COLLECTORS  
# ═══════════════════════════════════════════════════

class AudioDataCollector:
    """
    Collect speech, music, and ambient audio datasets.
    """
    
    def download_common_voice(self, languages: list[str] = None,
                               output_dir: str = "data/audio/common_voice"):
        """Mozilla Common Voice — 30,000+ hours in 120 languages"""
        from datasets import load_dataset
        
        langs = languages or ["en", "hi", "fr", "de", "es", "zh-CN", "ar", "ru"]
        Path(output_dir).mkdir(parents=True, exist_ok=True)
        
        for lang in langs:
            logger.info(f"Downloading Common Voice: {lang}")
            try:
                ds = load_dataset(
                    "mozilla-foundation/common_voice_17_0",
                    lang,
                    split="train",
                    trust_remote_code=True
                )
                ds.save_to_disk(f"{output_dir}/{lang}")
                logger.success(f"✓ Common Voice {lang}: {len(ds)} samples")
            except Exception as e:
                logger.error(f"Failed {lang}: {e}")
    
    def download_gigaspeech(self, subset: str = "l",
                              output_dir: str = "data/audio/gigaspeech"):
        """GigaSpeech — 10,000 hours diverse English speech"""
        from datasets import load_dataset
        
        # Subsets: xs(10h), s(250h), m(1000h), l(2500h), xl(10000h)
        ds = load_dataset("speechcolab/gigaspeech", subset, trust_remote_code=True)
        ds.save_to_disk(output_dir)
    
    def download_indic_speech(self, output_dir: str = "data/audio/indic"):
        """
        Best Hindi/Indic speech datasets.
        Critical for multilingual model training.
        """
        indic_datasets = [
            ("ai4bharat/Shrutilipi", None),      # 6,400 hrs Indic
            ("ai4bharat/indicSUPERB", None),      # Benchmark  
            ("google/fleurs", "hi_in"),            # Hindi FLEURS
            ("mozilla-foundation/common_voice_17_0", "hi"),  # Hindi CV
        ]
        
        Path(output_dir).mkdir(parents=True, exist_ok=True)
        for dataset_name, config in indic_datasets:
            try:
                ds = load_dataset(dataset_name, config, split="train",
                                  trust_remote_code=True)
                save_path = f"{output_dir}/{dataset_name.replace('/','_')}"
                ds.save_to_disk(save_path)
                logger.success(f"✓ {dataset_name}: {len(ds)} samples")
            except Exception as e:
                logger.error(f"Failed {dataset_name}: {e}")
    
    def transcribe_audio_whisper(self, audio_dir: str, 
                                   output_dir: str,
                                   model_size: str = "large-v3"):
        """
        Use OpenAI Whisper to transcribe any audio.
        Creates text data from audio files.
        """
        import whisper
        
        model = whisper.load_model(model_size)
        audio_files = list(Path(audio_dir).rglob("*.mp3")) + \
                      list(Path(audio_dir).rglob("*.wav")) + \
                      list(Path(audio_dir).rglob("*.m4a"))
        
        Path(output_dir).mkdir(parents=True, exist_ok=True)
        results = []
        
        for audio_file in audio_files:
            try:
                result = model.transcribe(str(audio_file), language=None)
                transcript = {
                    "file": str(audio_file),
                    "text": result["text"],
                    "language": result["language"],
                    "segments": result["segments"]
                }
                results.append(transcript)
                
                # Save individual transcript
                out_file = Path(output_dir) / f"{audio_file.stem}.json"
                out_file.write_text(json.dumps(transcript, indent=2))
                
                logger.info(f"Transcribed: {audio_file.name}")
            except Exception as e:
                logger.error(f"Failed: {audio_file}: {e}")
        
        return results


# ═══════════════════════════════════════════════════
# SECTION 5: HUGGING FACE MASS DOWNLOADER
# ═══════════════════════════════════════════════════

PRIORITY_HF_DATASETS = [
    # ── LARGE TEXT ─────────────────────────────────
    ("HuggingFaceFW/fineweb",              "CC-MAIN-2024-51",  "text"),
    ("HuggingFaceFW/fineweb-edu",          "sample-350BT",     "text"),
    ("allenai/dolma",                       "v1_7",             "text"),
    ("cerebras/SlimPajama-627B",            None,               "text"),
    ("togethercomputer/RedPajama-Data-1T",  "arxiv",            "text"),
    ("togethercomputer/RedPajama-Data-1T",  "book",             "text"),
    ("togethercomputer/RedPajama-Data-1T",  "github",           "text"),
    ("togethercomputer/RedPajama-Data-1T",  "wikipedia",        "text"),
    ("EleutherAI/pile",                     None,               "text"),
    ("c4",                                  "en",               "text"),
    ("wikimedia/wikipedia",                 "20231101.en",      "text"),
    ("wikimedia/wikipedia",                 "20231101.hi",      "text"),
    
    # ── CODE ───────────────────────────────────────
    ("bigcode/the-stack-dedup",            "data",             "code"),
    ("bigcode/starcoderdata",              None,               "code"),
    ("codeparrot/github-code",             None,               "code"),
    ("bigcode/jupyter-structured-clean-v1",None,               "code"),
    
    # ── INSTRUCTION/RLHF ───────────────────────────
    ("teknium/OpenHermes-2.5",             None,               "instruct"),
    ("Open-Orca/OpenOrca",                 None,               "instruct"),
    ("HuggingFaceH4/ultrachat_200k",       None,               "instruct"),
    ("HuggingFaceTB/smoltalk",             "all",              "instruct"),
    ("Magpie-Align/Magpie-Ultra-v0.1",     None,               "instruct"),
    ("WizardLM/WizardLM_evol_instruct_V2_196k", None,         "instruct"),
    ("google/flan",                        None,               "instruct"),
    ("OpenAssistant/oasst2",               None,               "instruct"),
    ("meta-math/MetaMathQA",               None,               "instruct"),
    ("databricks/databricks-dolly-15k",    None,               "instruct"),
    
    # ── MULTILINGUAL ───────────────────────────────
    ("uonlp/CulturaX",                     "hi",               "multilingual"),
    ("uonlp/CulturaX",                     "en",               "multilingual"),
    ("ai4bharat/sangraha",                 "verified",         "multilingual"),
    ("CohereForAI/aya_dataset",            None,               "multilingual"),
    ("CohereForAI/aya_collection",         None,               "multilingual"),
    ("oscar-corpus/OSCAR-2301",            "hi",               "multilingual"),
    
    # ── SCIENTIFIC ─────────────────────────────────
    ("allenai/peS2o",                      None,               "scientific"),
    ("allenai/s2orc",                      None,               "scientific"),
    ("openai/gsm8k",                       "main",             "math"),
    ("lighteval/MATH",                     None,               "math"),
    ("facebook/natural_questions",         None,               "qa"),
    
    # ── MULTIMODAL ─────────────────────────────────
    ("mint-1t/MINT-1T",                    None,               "multimodal"),
    ("BAAI/Recap-DataComp-1B",             None,               "image_text"),
    ("laion/laion_aesthetics_v2_5",        None,               "image_text"),
]


class HuggingFaceMassDownloader:
    """Download ALL priority HuggingFace datasets"""
    
    def __init__(self, output_dir: str = "data/huggingface", 
                 token: str = None):
        self.output_dir = Path(output_dir)
        self.token = token
        self.failed = []
        self.success = []
    
    def download_dataset(self, name: str, config: str, category: str,
                          streaming: bool = False, max_rows: int = None):
        from datasets import load_dataset
        
        save_path = self.output_dir / category / name.replace("/", "__")
        if config:
            save_path = save_path / config
        
        if save_path.exists() and any(save_path.iterdir()):
            logger.info(f"SKIP (exists): {name}")
            return True
        
        try:
            logger.info(f"Downloading: {name} [{config or 'default'}]")
            ds = load_dataset(
                name, config,
                split="train",
                streaming=streaming,
                token=self.token,
                trust_remote_code=True
            )
            
            if streaming:
                # Stream to parquet in batches
                import pyarrow as pa
                import pyarrow.parquet as pq
                save_path.mkdir(parents=True, exist_ok=True)
                pf = save_path / "data.parquet"
                writer = None
                buf = []
                total = 0
                for item in ds:
                    buf.append(item)
                    total += 1
                    if max_rows and total >= max_rows:
                        break
                    if len(buf) >= 50000:
                        batch = pa.Table.from_pylist(buf)
                        if writer is None:
                            writer = pq.ParquetWriter(pf, batch.schema)
                        writer.write_table(batch)
                        buf = []
                        logger.info(f"  {name}: {total} rows saved")
                if buf:
                    batch = pa.Table.from_pylist(buf)
                    if writer is None:
                        writer = pq.ParquetWriter(pf, batch.schema)
                    writer.write_table(batch)
                if writer:
                    writer.close()
            else:
                if max_rows:
                    ds = ds.select(range(min(max_rows, len(ds))))
                save_path.mkdir(parents=True, exist_ok=True)
                ds.save_to_disk(str(save_path))
                ds.to_parquet(str(save_path / "data.parquet"))
            
            size = sum(f.stat().st_size for f in save_path.rglob("*") 
                      if f.is_file()) / 1e9
            logger.success(f"✓ {name}: {size:.2f} GB")
            self.success.append(name)
            return True
            
        except Exception as e:
            logger.error(f"✗ {name}: {e}")
            self.failed.append((name, str(e)))
            return False
    
    def download_all(self):
        """Download everything in priority order"""
        total = len(PRIORITY_HF_DATASETS)
        for i, (name, config, category) in enumerate(PRIORITY_HF_DATASETS):
            logger.info(f"[{i+1}/{total}] {name}")
            self.download_dataset(
                name, config, category,
                streaming=True,
                max_rows=5_000_000  # 5M rows max per dataset
            )
        
        # Print summary
        print(f"\n✅ Success: {len(self.success)}")
        print(f"❌ Failed: {len(self.failed)}")
        for name, err in self.failed:
            print(f"  - {name}: {err[:80]}")


# ═══════════════════════════════════════════════════
# SECTION 6: SYNTHETIC DATA GENERATION
# (Fill gaps where real data unavailable)
# ═══════════════════════════════════════════════════

SYNTHETIC_PROMPTS = {
    "reasoning_chains": """
Generate {n} complex multi-step reasoning problems with complete solutions.
Domains: mathematics, logic puzzles, physics, programming, causal reasoning.
Each problem must require 5-10 explicit reasoning steps.
Include problems of varying difficulty (easy/medium/hard/expert).
FORMAT: JSON array. Each item: {{"problem": str, "steps": list[str], "answer": str, "difficulty": str, "domain": str}}
Return ONLY valid JSON. No markdown, no preamble.
""",
    "code_with_tests": """
Generate {n} programming tasks with complete implementations AND unit tests.
Languages: Python, JavaScript, TypeScript, Go, Rust (distribute evenly).
Include: data structures, algorithms, system design, API calls, ML code.
FORMAT: JSON array. Each item: {{"task": str, "language": str, "solution": str, "tests": str, "explanation": str, "complexity": str}}
Return ONLY valid JSON. No markdown, no preamble.
""",
    "scientific_qa": """
Generate {n} PhD-level Q&A pairs on topic: {domain}.
Questions should test deep conceptual understanding, not just facts.
Include derivations, comparisons, edge cases, and common misconceptions.
FORMAT: JSON array. Each item: {{"question": str, "answer": str, "key_concepts": list[str], "difficulty": str}}
Return ONLY valid JSON. No markdown, no preamble.
""",
    "hindi_bilingual": """
Generate {n} bilingual instruction-response pairs (Hindi + English).
Include: everyday tasks, technical topics, cultural context, Hinglish.
Each pair should feel natural in both languages.
FORMAT: JSON array. Each item: {{"hindi_instruction": str, "english_instruction": str, "hindi_response": str, "english_response": str, "topic": str}}
Return ONLY valid JSON. No markdown, no preamble.
""",
    "dario_amodei_inspired": """
Generate {n} high-quality training examples inspired by Dario Amodei's vision 
in "Machines of Loving Grace" — AI accelerating human progress.

Domains: biology research acceleration, mental health support, economic empowerment,
medical breakthroughs, scientific discovery, global development.

Each example should represent the kind of knowledge exchange that could compress
decades of human progress into years — the "country of geniuses" at work.

FORMAT: JSON array. Each item: {{"context": str, "question": str, "expert_response": str, "impact_domain": str}}
Return ONLY valid JSON. No markdown.
""",
    "tool_use_agents": """
Generate {n} realistic agentic task examples where an AI must plan and use tools.
Tools available: web_search, code_execution, file_read, api_call, calculator.
Tasks should require 3-10 tool uses to complete.
FORMAT: JSON array. Each item: {{"task": str, "plan": list[str], "tool_calls": list[dict], "final_answer": str}}
Return ONLY valid JSON. No markdown.
"""
}

SCIENTIFIC_DOMAINS = [
    "machine learning", "quantum physics", "molecular biology",
    "neuroscience", "organic chemistry", "number theory",
    "astrophysics", "immunology", "materials science",
    "epidemiology", "climate science", "genetics",
    "pharmacology", "structural biology", "computer architecture"
]


class MultiModelSyntheticFactory:
    """
    Generate synthetic data at scale using multiple AI APIs.
    Parallelizes across models for maximum throughput.
    Inspired by Anthropic's Constitutional AI data generation.
    """
    
    def __init__(self, api_keys: dict):
        self.keys = api_keys
        self.clients = {}
        
        if api_keys.get("anthropic"):
            from anthropic import AsyncAnthropic
            self.clients["claude"] = AsyncAnthropic(api_key=api_keys["anthropic"])
        
        if api_keys.get("openai"):
            import openai
            self.clients["openai"] = openai.AsyncOpenAI(api_key=api_keys["openai"])
        
        if api_keys.get("google"):
            import google.generativeai as genai
            genai.configure(api_key=api_keys["google"])
            self.clients["gemini"] = genai
    
    async def generate_claude(self, prompt: str) -> list:
        if "claude" not in self.clients:
            return []
        resp = await self.clients["claude"].messages.create(
            model="claude-opus-4-5",
            max_tokens=8000,
            messages=[{"role": "user", "content": prompt}]
        )
        text = resp.content[0].text.strip()
        text = text.replace("```json", "").replace("```", "").strip()
        try:
            return json.loads(text)
        except:
            return []
    
    async def generate_openai(self, prompt: str) -> list:
        if "openai" not in self.clients:
            return []
        resp = await self.clients["openai"].chat.completions.create(
            model="gpt-4o",
            max_tokens=8000,
            response_format={"type": "json_object"},
            messages=[
                {"role": "system", "content": "Return ONLY valid JSON arrays."},
                {"role": "user", "content": prompt}
            ]
        )
        try:
            data = json.loads(resp.choices[0].message.content)
            return data if isinstance(data, list) else list(data.values())[0]
        except:
            return []
    
    async def generate_batch(self, task_type: str, n_per_batch: int = 50,
                              total: int = 10000,
                              domain: str = None) -> list:
        """Run parallel generation across all available models"""
        
        template = SYNTHETIC_PROMPTS[task_type]
        all_results = []
        batches_needed = total // n_per_batch
        
        tasks = []
        for i in range(batches_needed):
            prompt = template.format(n=n_per_batch, domain=domain or "general")
            # Alternate between models
            if i % 2 == 0:
                tasks.append(self.generate_claude(prompt))
            else:
                tasks.append(self.generate_openai(prompt))
            
            # Process in chunks of 10 parallel calls
            if len(tasks) >= 10:
                results = await asyncio.gather(*tasks, return_exceptions=True)
                for r in results:
                    if isinstance(r, list):
                        all_results.extend(r)
                tasks = []
                logger.info(f"{task_type}: {len(all_results)} samples generated")
        
        # Process remaining
        if tasks:
            results = await asyncio.gather(*tasks, return_exceptions=True)
            for r in results:
                if isinstance(r, list):
                    all_results.extend(r)
        
        return all_results
    
    async def run_full_synthetic_pipeline(self, output_dir: str,
                                            total_per_task: int = 10000):
        """Generate ALL synthetic datasets"""
        Path(output_dir).mkdir(parents=True, exist_ok=True)
        
        tasks = [
            ("reasoning_chains", None),
            ("code_with_tests", None),
            ("hindi_bilingual", None),
            ("dario_amodei_inspired", None),
            ("tool_use_agents", None),
        ] + [("scientific_qa", domain) for domain in SCIENTIFIC_DOMAINS]
        
        for task_type, domain in tasks:
            name = f"{task_type}_{domain or 'all'}"
            out_file = Path(output_dir) / f"{name}.jsonl"
            
            if out_file.exists():
                logger.info(f"SKIP: {name}")
                continue
            
            logger.info(f"Generating: {name}")
            data = await self.generate_batch(
                task_type, n_per_batch=50,
                total=total_per_task, domain=domain
            )
            
            async with aiofiles.open(out_file, 'w') as f:
                for item in data:
                    await f.write(json.dumps(item) + "\n")
            
            logger.success(f"✓ {name}: {len(data)} samples saved")


# ═══════════════════════════════════════════════════
# SECTION 7: MASTER ORCHESTRATOR
# ═══════════════════════════════════════════════════

class AntiGravityOrchestrator:
    """
    Master controller. Run this and walk away.
    It will collect everything.
    """
    
    def __init__(self, config_path: str = "ag_config.json"):
        with open(config_path) as f:
            self.cfg = json.load(f)
        
        self.base_dir = Path(self.cfg.get("base_output_dir", "data"))
        self.base_dir.mkdir(parents=True, exist_ok=True)
    
    async def run_all_phases(self):
        console.print("""
[bold green]
╔══════════════════════════════════════════════════════╗
║     ⚡ ANTI-GRAVITY DATA COLLECTION INITIATED ⚡      ║
║     "A country of geniuses in a data center"         ║
║     — Dario Amodei, Machines of Loving Grace         ║
╚══════════════════════════════════════════════════════╝
[/bold green]""")
        
        phases = [
            # TEXT
            ("📚 HuggingFace Mass Download",
             lambda: HuggingFaceMassDownloader(
                 str(self.base_dir / "huggingface"),
                 self.cfg.get("hf_token")
             ).download_all()),
            
            # PAPERS
            ("🔬 arXiv All Papers",
             lambda: asyncio.run(ArXivMassCollector(
                 str(self.base_dir / "arxiv")
             ).collect_all())),
            
            # PAPERS CONTINUED
            ("📖 OpenAlex Snapshot",
             lambda: OpenAlexCollector(
                 self.cfg.get("email", "user@example.com"),
                 str(self.base_dir / "openalex")
             ).download_snapshot()),
            
            # IMAGES
            ("🖼️ Image Datasets",
             lambda: LAIONImageCollector().download_openimages(
                 str(self.base_dir / "images/openimages")
             )),
            
            # VIDEO
            ("🎬 Video Datasets",
             lambda: VideoDataCollector().download_webvid(
                 str(self.base_dir / "video/webvid")
             )),
            
            # AUDIO
            ("🔊 Audio Datasets",
             lambda: AudioDataCollector().download_common_voice(
                 output_dir=str(self.base_dir / "audio/common_voice")
             )),
            
            # HINDI AUDIO
            ("🎙️ Hindi/Indic Audio",
             lambda: AudioDataCollector().download_indic_speech(
                 str(self.base_dir / "audio/indic")
             )),
            
            # SYNTHETIC
            ("🤖 Synthetic Data Generation",
             lambda: asyncio.run(MultiModelSyntheticFactory(
                 api_keys={
                     "anthropic": self.cfg.get("anthropic_api_key"),
                     "openai": self.cfg.get("openai_api_key"),
                     "google": self.cfg.get("google_api_key"),
                 }
             ).run_full_synthetic_pipeline(
                 str(self.base_dir / "synthetic"),
                 total_per_task=self.cfg.get("synthetic_per_task", 10000)
             ))),
        ]
        
        results = {}
        for name, phase_fn in phases:
            console.print(f"\n[bold cyan]▶ {name}[/bold cyan]")
            try:
                phase_fn()
                results[name] = "✅ SUCCESS"
                console.print(f"[green]✓ {name} complete[/green]")
            except Exception as e:
                results[name] = f"❌ {str(e)[:100]}"
                console.print(f"[red]✗ {name}: {e}[/red]")
                # NEVER stop — move to next phase
        
        self._print_final_report(results)
    
    def _print_final_report(self, results: dict):
        table = Table(title="🏆 ANTI-GRAVITY COLLECTION COMPLETE", 
                     title_style="bold green")
        table.add_column("Phase", style="cyan", width=40)
        table.add_column("Status", style="white", width=15)
        table.add_column("Size on Disk", style="yellow", width=15)
        
        for name, status in results.items():
            size = self._get_dir_size(self.base_dir)
            table.add_row(name, status, f"{size:.1f} GB")
        
        console.print(table)
        
        total = self._get_dir_size(self.base_dir)
        console.print(f"\n[bold green]TOTAL DATA COLLECTED: {total:.1f} GB[/bold green]")
    
    def _get_dir_size(self, path: Path) -> float:
        try:
            return sum(f.stat().st_size for f in path.rglob("*") 
                      if f.is_file()) / 1e9
        except:
            return 0.0


# ═══════════════════════════════════════════════════
# ENTRY POINT
# ═══════════════════════════════════════════════════

if __name__ == "__main__":
    import typer
    app = typer.Typer()
    
    @app.command()
    def collect(
        config: str = typer.Option("ag_config.json", "--config"),
        phase: str = typer.Option("all", "--phase", 
                                    help="all/text/images/video/audio/synthetic")
    ):
        orchestrator = AntiGravityOrchestrator(config)
        asyncio.run(orchestrator.run_all_phases())
    
    app()
```

---

## ⚙️ CONFIG — ag_config.json

```json
{
  "base_output_dir": "./anti_gravity_data",
  "email": "your@email.com",

  "anthropic_api_key": "sk-ant-...",
  "openai_api_key": "sk-...",
  "google_api_key": "AIza...",
  "hf_token": "hf_...",
  "github_token": "ghp_...",

  "synthetic_per_task": 10000,
  "image_download_workers": 16,
  "max_concurrent_requests": 20,
  "request_delay_seconds": 0.5,

  "priorities": {
    "text": true,
    "images": true,
    "video": true,
    "audio": true,
    "code": true,
    "scientific": true,
    "multilingual": true,
    "synthetic": true
  }
}
```

---

## 📦 REQUIREMENTS — requirements.txt

```
# Core async
aiohttp>=3.9.0
aiofiles>=23.0.0
httpx>=0.27.0
asyncio

# HuggingFace
datasets>=2.19.0
huggingface-hub>=0.23.0
transformers>=4.41.0

# Data processing
pandas>=2.2.0
pyarrow>=16.0.0
polars>=0.20.0

# Images
img2dataset>=1.41.0
Pillow>=10.0.0
opencv-python>=4.9.0

# Video/Audio
yt-dlp>=2024.5.0
whisper>=20231117
pydub>=0.25.0
ffmpeg-python>=0.2.0

# Web scraping
warcio>=1.7.4
beautifulsoup4>=4.12.0
lxml>=5.0.0
playwright>=1.44.0
fake-useragent>=1.5.0

# Deduplication
datasketch>=1.6.5
simhash>=2.1.0

# NLP
langdetect>=1.0.9
ftfy>=6.2.0
fasttext>=0.9.3

# PDFs
pymupdf>=1.24.0
pdfplumber>=0.11.0

# Scientific
biopython>=1.83

# APIs
anthropic>=0.30.0
openai>=1.35.0
google-generativeai>=0.7.0

# CLI/Monitoring
rich>=13.7.0
loguru>=0.7.0
typer>=0.12.0
tqdm>=4.66.0

# Storage
sqlalchemy>=2.0.0

# AWS (for Common Crawl, OpenAlex S3)
boto3>=1.34.0
awscli>=1.32.0
```

---

## 🚀 EXECUTION

```bash
# Install everything
pip install -r requirements.txt
playwright install chromium

# Configure
cp ag_config.json.example ag_config.json
# Edit with your API keys

# Run EVERYTHING (unattended)
python collect_everything.py collect --phase all

# Or run specific phases
python collect_everything.py collect --phase text
python collect_everything.py collect --phase images
python collect_everything.py collect --phase video
python collect_everything.py collect --phase audio
python collect_everything.py collect --phase synthetic

# Monitor progress
watch -n 30 du -sh anti_gravity_data/*/
```

---

## 📊 EXPECTED YIELD

```
DATA TYPE       │ SOURCE COUNT │ EXPECTED SIZE  │ QUALITY
────────────────┼──────────────┼────────────────┼──────────────
Text/NLP        │ 25+ sources  │ 5-15 TB        │ ████████████
Academic Papers │ 12+ sources  │ 500 GB - 2 TB  │ ████████████
Books           │ 8+ sources   │ 100-500 GB     │ ████████████
Code            │ 8+ sources   │ 1-5 TB         │ ████████████
Images          │ 12+ sources  │ 2-10 TB        │ ████████████
Video           │ 8+ sources   │ 5-50 TB        │ ████████████
Audio/Speech    │ 10+ sources  │ 1-5 TB         │ ████████████
Multilingual    │ 10+ sources  │ 1-3 TB         │ ████████████
Scientific      │ 8+ sources   │ 200-500 GB     │ ████████████
Synthetic       │ 4 AI models  │ 50-200 GB      │ ████████████
────────────────┼──────────────┼────────────────┼──────────────
TOTAL           │ 100+ sources │ 15-90 TB       │ MAXIMUM
```

---

## 🧠 DARIO AMODEI'S VISION — DATA STRATEGY

```
From "Machines of Loving Grace" (October 2024, 15,000 words):

"Think about what it means to have access to a brilliant friend who 
happens to have the knowledge of a doctor, lawyer, financial advisor, 
and expert in whatever you need."

THIS IS YOUR TRAINING DATA MISSION:
Every paper that could cure a disease     → PubMed + bioRxiv
Every book that could educate someone     → Gutenberg + Archive.org  
Every code that could build something     → GitHub + The Stack
Every conversation that could help someone → OpenAssistant + ShareGPT
Every language spoken on Earth            → CulturaX + Common Voice
Every image that represents our world     → LAION-5B + OpenImages
Every video that shows how things work    → Kinetics + HowTo100M

Compress decades of human knowledge into 
training data that accelerates progress.
That is the ANTI-GRAVITY mission.
```

---

*ANTI-GRAVITY-OMEGA | GOD MODE ULTRA PRO MAX ∞*
*Mindsets: Amodei × Karpathy × Huang × Altman × LeCun × Musk × Einstein × Turing*
*"Train not just on what humans know, but on what they could discover."*
